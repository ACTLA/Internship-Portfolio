// Генерация матрицы 400×800 распределения Пуассона
data = grand(400, 800, "poi", 2);

// Построение гистограммы и полигона
scf();
histplot(20, data, polygon = %t);
xtitle("Распределение Пуассона (mu=2)", "Значения", "Частота");
legend(["Гистограмма"; "Полигон"]);
xgrid();
